// Placeholder firebase config for builds without Firebase.
// If you have a real Firebase project, replace the exports below with your firebase/app initialization

// Export a dummy `db` object so imports resolve during build.
export const db = {} as any;
